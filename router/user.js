const UserController = require('../controller/user')
module.exports = (router, jwt) => {
    // router
    //     .get('/userinfo', jwt, UserController.userInfo)
    //     .get('/viplist', jwt, UserController.vipList)
}